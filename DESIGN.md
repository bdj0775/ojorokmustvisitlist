# 오조록 맛집 목록 — 디자인 시스템 안내서

제주 성산 숙소 '오조록'이 손님에게 주는 맛집 목록 사이트입니다.
이 문서 하나만 읽으면 UI를 고칠 수 있게 정리했습니다.

- **실제 사이트** — https://ojorokmustvisitlist.vercel.app/
- **살아있는 디자인 문서** — 로컬에서 `npm run dev` 후 `http://localhost:5173/styleguide.html`
  (토큰 파일을 직접 읽어 그리므로, 이 문서와 실제가 어긋나면 그쪽이 맞습니다)

---

## 0. 30초 요약

| 무엇을 바꾸고 싶은가 | 열 파일 |
|---|---|
| 색, 글자 크기, 간격, 모서리, 그림자 | `styles/tokens.css` **← 대부분 여기서 끝납니다** |
| 버튼·카드·태그·칩의 생김새 | `styles/components.css` |
| 요소를 어디에 얼마나 띄울지 | `styles/layout.css` |
| 글꼴, 기본 제목 크기 | `styles/base.css` |
| 화면에 나오는 문구 | `config/strings.mjs` (한/영/중) |
| 태그 이름과 색 그룹 | `config/tags.mjs` |

**가장 중요한 규칙: 색이나 치수를 바꿀 때 `components.css`를 열지 마세요.**
`tokens.css` 한 곳만 고치면 전체에 반영됩니다.

---

## 1. 이 사이트가 하려는 것

숙소 손님이 **휴대폰으로, 밥 먹기 직전에, 서서** 봅니다.
그래서 아래 판단이 깔려 있습니다. 디자인을 바꿀 때 이 전제를 먼저 확인해 주세요.

- **모바일이 기준입니다.** 390px 폭에서 먼저 확인하고, 넓은 화면은 그 다음입니다.
- **훑어보는 화면입니다.** 카드 한 장이 낮을수록 한 화면에 더 많은 곳이 들어옵니다.
  실제로 카드에서 사진·주소·거리 배지를 덜어내 249px → 136px로 줄인 이력이 있습니다.
- **광고가 아니라 개인 추천입니다.** 상업적인 화려함보다 담백함을 지향합니다.
- **제주 햇빛 아래에서 읽힙니다.** 그래서 색 대비 기준(4.5:1)을 도구로 강제합니다.

---

## 2. 파일 구조

```
styles/
  tokens.css       ← 1. 모든 색·치수의 유일한 출처
  base.css         ← 2. 리셋 + 글꼴·제목 기준
  components.css   ← 3. 재사용 부품 (버튼·카드·태그·칩)
  layout.css       ← 4. 이 페이지만의 배치 (헤더·히어로·푸터)
  admin.css        ←    주인장 전용 화면 (손님에게 안 보임)
```

**불러오는 순서가 곧 우선순위입니다.** `index.html`에서 이 순서를 바꾸지 마세요.
뒤에 오는 파일이 앞의 값을 덮어씁니다.

---

## 3. 토큰 — 3단 구조

`tokens.css`는 3층으로 나뉩니다. 이 구조 덕분에 다크 모드가 블록 하나로 끝납니다.

```
(1) 원시 토큰    --blue-500        순수한 색값. 이름에 용도가 없음
        ↓
(2) 의미 토큰    --color-brand     용도에 이름을 붙인 것 ← 컴포넌트는 이것만 사용
        ↓
(3) 컴포넌트 토큰 --btn-height-md   특정 부품 전용 치수
```

**철칙: `components.css`에서 `--blue-500` 같은 원시 토큰을 직접 쓰지 마세요.**
반드시 `--color-brand` 같은 의미 토큰을 씁니다. 이걸 어기면 다크 모드가 깨집니다.

### 바꾸는 방법

| 하고 싶은 것 | 고칠 층 | 결과 |
|---|---|---|
| "브랜드 색을 초록으로" | (2) `--color-brand` 한 줄 | 버튼·링크·지도 마커가 한꺼번에 따라옴 |
| "파랑을 더 청록으로" | (1) `--blue-*` 스케일 | 명암 관계가 유지된 채 전부 반영 |
| "버튼을 더 크게" | (3) `--btn-height-md` | 모든 기본 버튼이 커짐 |

---

## 4. 색

### 팔레트

세 갈래입니다. 각 색은 50(밝음)~900(어두움) 스케일을 가집니다.

| 이름 | 뜻 | 쓰이는 곳 |
|---|---|---|
| **blue** | 제주 바다 | 브랜드. 링크, 보조 버튼, 지도 |
| **orange** | 감귤 | 강조. 예약처럼 가장 중요한 행동 **하나에만** |
| **neutral** | 현무암 (살짝 따뜻한 회색) | 글자, 배경, 선 |
| sand / green | 보조 | 태그 색으로만 |

### 의미 토큰 (컴포넌트가 실제로 쓰는 것)

```
배경   --color-bg  --color-surface  --color-surface-sunken
글자   --color-text  --color-text-muted  --color-text-subtle  --color-text-inverse
선     --color-border  --color-border-strong
브랜드 --color-brand  --color-brand-hover  --color-brand-subtle  --color-brand-text  --color-on-brand
강조   --color-accent  --color-accent-hover  --color-accent-subtle  --color-accent-text  --color-on-accent
반전   --color-inverse-bg  --color-inverse-text  --color-inverse-heading   (푸터)
포커스 --color-focus-ring  --focus-ring
태그   --tag-place-*  --tag-kind-*  --tag-vibe-*
```

### ⚠️ 감귤색 위의 글자는 흰색이 아닙니다

`--color-on-accent`는 흰색이 아니라 **진한 현무암색(neutral-900)** 입니다.
선명한 감귤색 위의 흰 글자는 대비가 3.09:1밖에 안 나와 기준(4.5:1) 미달입니다.
진한 글자로 두면 5.57:1이 나오면서 색의 선명함도 지킵니다.

같은 이유로 `--color-accent-hover`는 **어둡게가 아니라 밝게**(orange-400) 잡혀 있습니다.
눌렀을 때도 대비가 유지되어야 하기 때문입니다.

---

## 5. 타이포

글꼴은 **Pretendard**입니다. 바꾸려면 `tokens.css`의 `--font-sans`와
`index.html`의 `<link>` 두 곳을 함께 교체하세요.

| 토큰 | 크기 | 쓰임 |
|---|---|---|
| `--text-2xs` | 11px | 아주 작은 라벨 |
| `--text-xs` | 12px | 태그·배지 |
| `--text-sm` | 13px | 보조 설명, 버튼 |
| `--text-md` | 15px | **본문 기본** |
| `--text-lg` | 17px | 카드 제목 |
| `--text-xl` | 20px | 섹션 제목 |
| `--text-2xl` | 24px | 히어로 제목(모바일) |
| `--text-3xl` | 30px | 히어로 제목(넓은 화면) |

굵기는 `--weight-regular`(400) ~ `--weight-extrabold`(800).
카드 제목과 브랜드명에 extrabold를 씁니다.

행간은 `--leading-tight`(1.25) ~ `--leading-relaxed`(1.75), 본문 기본은 `--leading-normal`(1.6).

---

## 6. 간격·모서리·그림자

**간격은 4px 배수입니다.** `--space-1`(4px)부터 `--space-16`(64px)까지.
임의의 px 값을 쓰지 말고 토큰을 쓰세요.

모서리: `--radius-xs`(6px) ~ `--radius-xl`(20px), `--radius-full`(완전한 원)
그림자: `--shadow-xs` ~ `--shadow-lg` (현무암색 기반, 다크 모드에서 따로 재정의됨)

레이아웃 상수:
- `--content-max` 680px — 본문 최대 폭
- `--header-height` 56px — 고정 헤더 높이
- `--page-padding` 12px (넓은 화면 16px)

---

## 7. 컴포넌트

이름 규칙은 BEM입니다.

```
.블록          부품 자체        .btn
.블록__요소     부품의 부분      .card__title
.블록--변형     부품의 변형      .btn--accent
```

### 버튼 `.btn`

| 변형 | 생김새 | 언제 |
|---|---|---|
| `--accent` | 감귤색 채움 | 예약. **한 화면에 하나가 원칙** |
| `--brand` | 파란색 채움 | 지도 앱으로 가기 |
| `--outline` | 테두리만 | 공유하기, 인스타그램 |
| `--ghost` | 배경 없음 | 가장 약한 행동 |

크기 `--sm`(32px) / 기본(40px) / `--lg`(46px), 폭 `--block`(꽉 참) / `--grow`(남는 공간 나눔).
모서리는 `--radius-full`이라 **알약 모양**입니다. 누르면 `scale(0.97)`로 살짝 눌립니다.

### 카드 `.card`

목록을 훑는 화면이라 **밀도가 곧 쓸모**입니다. 네 덩어리만 담습니다:

```
┌────────────────────────────────────┐
│ 쌈총사횟집        [맛집][횟집][현지인] │ ← 이름 왼쪽, 태그 오른쪽 끝
│ 여러 종의 횟집. 깔끔하게 관리하심      │ ← 한 줄 소개 (2줄에서 잘림)
│ 추천 · 갈치조림, 모둠회               │ ← 추천 메뉴 (없으면 이 줄만 빠짐)
│ [네이버지도] [구글맵]                 │ ← 길찾기
└────────────────────────────────────┘
```

**태그가 오른쪽 끝 정렬인 이유**: 카드마다 태그 수가 달라도 오른쪽 끝이 맞아서,
목록을 내리는 동안 시선이 좌우로 흔들리지 않습니다.

**높이 조절은 토큰 세 줄로 합니다** — `--card-padding-y` / `--card-padding-x` / `--card-gap`.

### 태그 `.tag`

**`components.css`에 태그 이름이 하나도 없습니다.**
색은 태그가 속한 **그룹**으로만 정해집니다:

| 그룹 | 색 | 예시 |
|---|---|---|
| `place` (업종) | 파랑 | 맛집, 카페, 베이커리 |
| `kind` (음식) | 주황 | 횟집, 갈치, 메밀 |
| `vibe` (특징) | 초록 | 현지인맛집, 오션뷰, 가성비 |

태그를 100개로 늘려도 CSS는 그대로입니다. `config/tags.mjs`에 한 줄 추가하면 끝입니다.
**이 3색 제한이 카드가 알록달록해지는 걸 막습니다.** 색을 더 늘리자는 제안은 이 판단과 충돌합니다.

### 그 밖

- `.chip` — 필터 (가로 스크롤, 선택 시 반전)
- `.segmented` — 언어 전환 KR/EN/中
- `.switch` — '가까운 순' 정렬 토글
- `.badge` — 거리 표시 (**현재 화면에서 쓰이지 않음**, 스타일만 남아 있음)
- `.disclosure` — 지도 접기/펴기
- `.empty-state` — 결과 없음

---

## 8. 화면 구조

```
헤더        고정. 브랜드 + 언어전환 + 예약 버튼
히어로      제목 + 소개글 + [공유하기][예약하기].  배경은 파란색 → 배경색 그라데이션
지도        접을 수 있음.  ⚠️ 지금은 자동으로 숨겨져 있음 (아래 참고)
필터바      고정(헤더 아래). 태그 칩 가로 스크롤 + 정렬 스위치
카드 목록    본문
푸터        어두운 배경. [에어비앤비][네이버예약][인스타그램]
```

브레이크포인트는 **하나뿐입니다** — `45rem`(720px).
그 위에서 히어로가 커지고 지도가 높아지고 페이지 여백이 넓어집니다.

---

## 9. 지금 알아야 할 상태 (2026-08 기준)

디자인을 만지기 전에 알아야 할 것들입니다.

### 🟢 지도·정렬은 이제 동작합니다

37곳 좌표가 모두 채워져 지도에 핀이 뜨고, '가까운 순' 정렬도 됩니다.
정렬은 `distance_min`(차로 몇 분)이 있으면 그 값을, 없으면 **좌표로 직선거리를 계산**해 씁니다.
좌표가 하나도 없으면 지도 영역이 통째로 자동으로 숨겨집니다.

**정렬을 켜면 카드 오른쪽 위의 주소가 거리(`1.6km`)로 바뀝니다.**
왜 이 순서인지 근거를 보여주면서도 줄을 늘리지 않으려는 처리입니다.

### 🟡 지도 그림은 OpenStreetMap 입니다

핀을 찍는 프로그램은 Leaflet, 지도 그림은 OpenStreetMap 입니다.
한국은 데이터가 성겨서 **가게 이름이 지도에 표시되지 않습니다.**
네이버 지도로 바꾸면 상호가 보이지만 별도 키가 필요합니다.

### 🟡 카드에서 뺀 것들

카드 압축 때 **사진·거리 배지**를 화면에서 뺐습니다.
데이터(`photo`)와 CSS(`.badge`)는 그대로 남아 있으니
되살리려면 `app.js`의 `placeCard()`에 다시 넣으면 됩니다.
(주소는 이후 카드 오른쪽 위에 다시 들어왔습니다)

### 🟡 사진이 한 장도 없습니다

`images/` 폴더에 맛집 사진이 한 장도 없습니다.
사진을 쓰는 디자인을 제안하려면 이 점을 감안해 주세요.

카톡 공유 미리보기(`images/og.png`)는 **임시로 만든 것**입니다 —
사진이 없어 사이트 색 위에 글자만 얹었습니다. 여기는 디자인을 손볼 여지가 큽니다.

---

## 10. 지켜야 할 제약

### 색 대비 4.5:1은 도구로 강제됩니다

`npm run check`가 **32개 색 조합**(밝은 모드 16 + 다크 모드 16)을 계산해
WCAG AA 기준 미달이면 알려줍니다. 색을 바꿨다면 반드시 돌려보세요.

```powershell
npm run check
```

검사 목록은 `tools/check-contrast.mjs` 위쪽 `PAIRS` 배열에 있습니다.
새 색 조합을 만들었다면 여기에 한 줄 추가해 주세요.

### 다크 모드를 함께 확인하세요

`tokens.css` 아래쪽에 `@media (prefers-color-scheme: dark)` 블록이 있습니다.
**의미 토큰만 다시 매핑**하고 원시 토큰과 컴포넌트 CSS는 손대지 않습니다.
색을 추가했다면 이 블록에도 짝을 넣어야 합니다.

밝은 가이드북 느낌만 원한다면 이 블록을 통째로 지우면 됩니다.

### 외부 패키지를 쓰지 않습니다

빌드 과정이 없는 순수 HTML/CSS/JS입니다. `npm install` 자체가 필요 없습니다.
Tailwind, SCSS, 컴포넌트 라이브러리는 이 구조와 맞지 않습니다.
예외는 CDN으로 불러오는 **Pretendard**(글꼴)와 **Leaflet**(지도) 둘뿐입니다.

### 접근성

- 포커스 링(`:focus-visible`)은 키보드 사용자에게 필수입니다. 지우지 마세요.
- `prefers-reduced-motion`을 존중합니다 — 애니메이션을 추가한다면 이 설정도 확인하세요.
- 색만으로 정보를 전달하지 마세요 (태그는 색 + 글자를 함께 씁니다).

### 다국어

한국어 / 영어 / 중국어(번체) 3개 국어입니다.
문구를 새로 만들면 `config/strings.mjs`의 **3개 언어를 모두** 채워주세요.
en/zh가 비면 한국어가 자동으로 대신 나오므로 화면이 깨지진 않습니다.

**중국어·영어는 한국어보다 길어집니다.** 버튼이나 칩 폭을 고정하지 마세요.

### 주석은 한국어 평서문으로

이 저장소의 주석은 개발자가 아닌 주인장이 읽습니다.
"무엇을 했는지"가 아니라 **"왜 그렇게 했는지"** 를 적습니다.
코드를 고치면 같은 톤으로 주석도 맞춰주세요.

---

## 11. 확인하는 법

```powershell
npm run dev      # http://localhost:5173
npm run check    # 데이터 + 색 대비 검사 (색을 바꿨다면 필수)
```

- 손님 화면 — `http://localhost:5173/`
- **디자인 문서** — `http://localhost:5173/styleguide.html` (토큰을 직접 읽어 그림)
- 주인장 화면 — `http://localhost:5173/admin.html` (손님에게 안 보이는 페이지)

확인 순서: **390px 모바일 먼저 → 다크 모드 → 넓은 화면**.

---

## 12. 고칠 때 자주 만나는 상황

| 상황 | 어디를 고치나 |
|---|---|
| 브랜드 색 전체 변경 | `tokens.css` (2)층 `--color-brand*` |
| 카드를 더 낮게/높게 | `tokens.css` `--card-padding-y`, `--card-gap` |
| 버튼 모양을 사각형으로 | `tokens.css` `--btn-radius` |
| 태그 색 3종 변경 | `tokens.css` `--tag-*-bg` / `--tag-*-fg` (다크 모드 블록도 함께) |
| 새 태그 추가 | `config/tags.mjs`에 한 줄 (CSS 수정 불필요) |
| 카드에 사진 되살리기 | `app.js`의 `placeCard()` + `components.css`에 `.card__photo` 추가 |
| 문구 변경 | `config/strings.mjs` (3개 언어 모두) |
| 화면 폭 대응 추가 | `layout.css` 맨 아래 `@media (min-width: 45rem)` |
